# schema: http://backend-example.codebootcamp.co.kr/graphql
# schema 주소 -> 백엔드 주소로 변경
generates:
  ./src/commons/types/generated/types.ts:
    plugins:
      - typescript
    config:
      typesPrefix: I
